<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Membaca daftar aplikasi lain. Sejak Android 11 visibilitas paket dibatasi,
         tanpa <queries> ini daftar aplikasi akan tampak kosong. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- Memasang berkas APK hasil clone lewat pemasang sistem. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".App"
        android:label="ClonApk"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Trampolin ikon layar utama untuk aplikasi di dalam mesin parallel:
             launcher bisa membukanya, ia menjalankan app guest lalu menutup. -->
        <activity
            android:name=".ParallelShortcutActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay" />

        <!-- Menerima hasil sesi PackageInstaller (pemasangan multi-berkas
             base+split) dan membuka layar konfirmasi pemasangan sistem. -->
        <receiver
            android:name=".ClonApkInstallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.clonapk.app.INSTALL_DONE" />
            </intent-filter>
        </receiver>

        <!-- Dibutuhkan ruang virtual: menerima penyediaan profil kerja sehingga
             ClonApk menjadi profile owner dan bisa memasang instance kedua
             sebuah aplikasi (package sama) tanpa memodifikasi APK. -->
        <receiver
            android:name=".ClonApkDeviceAdmin"
            android:exported="false"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Diperlukan untuk memasang/membagikan APK pada Android 7.0+
             karena URI file:// ditolak. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
